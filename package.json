{
  "name": "autochain",
  "version": "0.1.0",
  "description": "AutoChain — recuperación de vehículos robados con IA y recompensas en stablecoin sobre HSK Chain.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:hsk-testnet": "hardhat run scripts/deploy.js --network hskTestnet",
    "deploy:hsk-mainnet": "hardhat run scripts/deploy.js --network hskMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
